@model GestorHorarioG6.Models.Requisicao

@{
    ViewData["Title"] = "Editar";
}

<h2>Editar</h2>

<h4>Requisição</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="RequisicaoId" />
            <div class="form-group">
                <label asp-for="DepartamentoId" class="control-label">Serviço</label>
                <select asp-for="DepartamentoId" class="form-control" asp-items="ViewBag.Departamento"></select>
            </div>
            <div class="form-group">
                <label asp-for="Detalhes" class="control-label">Hora de Início</label>
                <input asp-for="Detalhes" class="form-control" />
                <span asp-validation-for="Detalhes" class="text-danger"></span>
            </div>
            <div class="form-group">
                <a asp-action="Index"><input type="button" value="Voltar" class="btn btn-default" /></a>
                <input type="submit" value="Guardar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

