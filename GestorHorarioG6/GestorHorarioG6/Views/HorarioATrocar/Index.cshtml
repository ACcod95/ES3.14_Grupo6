@model IEnumerable<GestorHorarioG6.Models.HorarioATrocar>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HorarioTecnicos)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HorarioTecnicos.HorarioTecnicoId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HorarioATrocarId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HorarioATrocarId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HorarioATrocarId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
